{
  "name": "web-slides",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "slidev build --base /web-slides/",
    "dev": "slidev --open",
    "export": "slidev export --format pdf --per-slide",
    "export-pptx": "slidev export --format pptx --per-slide"
  },
  "dependencies": {
    "@slidev/cli": "^51.6.0",
    "@slidev/theme-default": "^0.25.0"
  },
  "devDependencies": {
    "playwright-chromium": "^1.52.0"
  }
}
